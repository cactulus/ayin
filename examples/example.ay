#use "libc";
#use "array";
#use "glfw";

func main() s32 {
	if !glfwInit() {
	}

    win := glfwCreateWindow(600, 480, "Test".data, nil, nil);

    glfwMakeContextCurrent(win);

    while !glfwWindowShouldClose(win) {
        glfwPollEvents();

        glfwSwapBuffers(win);
    }

	return 0;
}
